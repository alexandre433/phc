pack benches.fib;

function fib(int $n): int {
    if ($n < 2) { return $n; }
    return fib($n - 1) + fib($n - 2);
}

function main(): void {
    int $r = fib(35);
    Logger::info("fib(35) = {$r}");
}
